---
title: 'count sql query in php'
description: 'count sql query in php'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/php.png'
category: 'php'
tags: [count, php, sql, coun]
---

**The solution for this is noted below**

## count sql query in php

### Solution

```php
$sql = "SELECT COUNT(*) AS total from Members";
$result = $conn->query($sql);
$data =  $result->fetch_assoc();
echo $data['total'];
```

**Try other methods by searching on the site. That is if this doesn't work**
