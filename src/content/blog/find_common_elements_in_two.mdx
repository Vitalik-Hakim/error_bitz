---
title: 'find common elements in two lists python'
description: 'find common elements in two lists python'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags:
  [
    elements,
    find,
    python,
    whatever,
    list,
    whatev,
    arrays,
    ar,
    typescript,
    javascript,
    between,
    items,
    java
  ]
---

**The solution for this is noted below**

## find common elements in two lists python

### Solution

```python
list1 = [1,2,3,4,5,6]
list2 = [3, 5, 7, 9]
list(set(list1).intersection(list2))
```

**Try other methods by searching on the site. That is if this doesn't work**
