---
title: 'pandas create empty dataframe'
description: 'pandas create empty dataframe'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [dataframe, pandas, python, data, frame, fram]
---

**The solution for this is noted below**

## pandas create empty dataframe

### Solution

```python
# Basic syntax:
import pandas as pd
empty_dataframe = pd.DataFrame()

# Create empty dataframe with column names
empty_dataframe = pd.DataFrame(columns=['your', 'column', 'names'])

# Create empty dataframe with row names
empty_dataframe = pd.DataFrame(index=['your', 'row', 'names'])
```

**Try other methods by searching on the site. That is if this doesn't work**
