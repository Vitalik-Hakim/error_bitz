---
title: '`FirebaseAnalytics` requires CocoaPods version `>= 1.10.0`, which is not satisfied by your current version, `1.5.2`.'
description: '`FirebaseAnalytics` requires CocoaPods version `>= 1.10.0`, which is not satisfied by your current version, `1.5.2`.'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [cocoapods, requires, shell, version]
---

**The solution for this is noted below**

## `FirebaseAnalytics` requires CocoaPods version `>= 1.10.0`, which is not satisfied by your current version, `1.5.2`.

### Solution

```shell
sudo gem install cocoapods --pre
```

**Try other methods by searching on the site. That is if this doesn't work**
