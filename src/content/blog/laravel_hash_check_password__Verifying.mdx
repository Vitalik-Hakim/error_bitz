---
title: 'laravel hash check password, Verifying That A Password Matches A Hash'
description: 'laravel hash check password, Verifying That A Password Matches A Hash'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/php.png'
category: 'php'
tags: [hash, laravel, php]
---

**The solution for this is noted below**

## laravel hash check password, Verifying That A Password Matches A Hash

### Solution

```php
if (Hash::check('plain-text', $hashedPassword)) {
    // The passwords match...
}
```

**Try other methods by searching on the site. That is if this doesn't work**
