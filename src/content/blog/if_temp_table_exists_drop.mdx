---
title: 'if temp table exists drop'
description: 'if temp table exists drop'
pubDate: 'Apr 17 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/typescript.png'
category: 'typescript'
tags: [drop, exists, typescript, sql, ex, exis]
---

**The solution for this is noted below**

## if temp table exists drop

### Solution

```typescript
if object_id(N'tempdb..#MaxTransaction') is not null drop table #MaxTransaction
```
**Try other methods by searching on the site. That is if this doesn't work**
