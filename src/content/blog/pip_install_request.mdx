---
title: 'pip install request'
description: 'pip install request'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [pip, request, shell]
---

**The solution for this is noted below**

## pip install request

### Solution

```shell
pip install request:
python -m pip install requests
```

**Try other methods by searching on the site. That is if this doesn't work**
