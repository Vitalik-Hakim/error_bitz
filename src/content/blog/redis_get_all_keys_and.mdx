---
title: 'redis get all keys and values python'
description: 'redis get all keys and values python'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [get, python, redis]
---

**The solution for this is noted below**

## redis get all keys and values python

### Solution

```python
#using Pyredis library:

import redis

r = redis.Redis("localhost", 6379)
for key in r.scan_iter():
       print key
```

**Try other methods by searching on the site. That is if this doesn't work**
