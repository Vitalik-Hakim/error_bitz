---
title: 'uninstall app via adb'
description: 'uninstall app via adb'
pubDate: 'Apr 17 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [adb, shell]
---

**The solution for this is noted below**

## uninstall app via adb

### Solution

```shell
//access adb shell to avoid repeating adb shell in all commands as repeated below
adb shell 
//list installed packages
adb shell pm list packages
// find package
adb shell pm list packages | grep '<OEM/Carrier/App Name>'
// unstall 
adb shell pm uninstall -k --user 0 NameOfPackage
//or as a single command
adb shell pm uninstall --user 0 <package name>
```
**Try other methods by searching on the site. That is if this doesn't work**
