---
title: 'export csv in r'
description: 'export csv in r'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/R.png'
category: 'r'
tags: [export-csv, r, e]
---

**The solution for this is noted below**

## export csv in r

### Solution

```r
# to store in the current directory
write.csv(data, "data.csv")

# to store in a separate directory
write.csv(data, "file_path\\data.csv")
```

**Try other methods by searching on the site. That is if this doesn't work**
