---
title: 'install node on linux instance'
description: 'install node on linux instance'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [instance, linux, shell]
---

**The solution for this is noted below**

## install node on linux instance

### Solution

```shell
#Install node version manager (nvm) by typing the following at the command line.

curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.34.0/install.sh | bash

#Activate nvm by typing the following at the command line.

. ~/.nvm/nvm.sh

#Use nvm to install the latest version of Node.js by typing the following at the command line.
nvm install node
```

**Try other methods by searching on the site. That is if this doesn't work**
