---
title: 'error: can`t find python executable "python", you can set the python env variable.'
description: 'error: can`t find python executable "python", you can set the python env variable.'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [env, find, python, set, shell]
---

**The solution for this is noted below**

## error: can`t find python executable "python", you can set the python env variable.

### Solution

```shell
apt-get update
apt-get install python2.7
ln -s /usr/bin/python2.7 /usr/bin/python
```

**Try other methods by searching on the site. That is if this doesn't work**
