---
title: 'reverse one hot encoding python numpy'
description: 'reverse one hot encoding python numpy'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [numpy, one-hot-encoding, python, reverse]
---

**The solution for this is noted below**

## reverse one hot encoding python numpy

### Solution

```python
import numpy as np
class_labels = np.argmax(class_prob, axis=1) # assuming you have n-by-5 class_prob
```

**Try other methods by searching on the site. That is if this doesn't work**
