---
title: 'Extracting specific data from a string with regex and Powershell'
description: 'Extracting specific data from a string with regex and Powershell'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [powershell, regex, shell, string]
---

**The solution for this is noted below**

## Extracting specific data from a string with regex and Powershell

### Solution

```shell
$found = $string -match '.*spid="(\d+)".*'
if ($found) {
    $spid = $matches[1]
}
```

**Try other methods by searching on the site. That is if this doesn't work**
