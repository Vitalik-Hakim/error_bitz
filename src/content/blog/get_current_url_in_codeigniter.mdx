---
title: 'get current url in codeigniter'
description: 'get current url in codeigniter'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/php.png'
category: 'php'
tags: [codeigniter, get, php, url]
---

**The solution for this is noted below**

## get current url in codeigniter

### Solution

```php
simple get like this

echo $this->input->get('my_id');
Load the URL helper To get current url

$currentURL = current_url(); //http://myhost/main

$params   = $_SERVER['QUERY_STRING']; //my_id=1,3

$fullURL = $currentURL . '?' . $params;

echo $fullURL;   //http://myhost/main?my_id=1,3
```

**Try other methods by searching on the site. That is if this doesn't work**
