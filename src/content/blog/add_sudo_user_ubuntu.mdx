---
title: 'add sudo user ubuntu'
description: 'add sudo user ubuntu'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [add, shell, sudo, ubuntu, shel]
---

**The solution for this is noted below**

## add sudo user ubuntu

### Solution

```shell
# add a user and set a password
adduser USERNAME

# add user to sudoers / sudo groupe with command below
usermod -aG sudo USERNAME
```

**Try other methods by searching on the site. That is if this doesn't work**
