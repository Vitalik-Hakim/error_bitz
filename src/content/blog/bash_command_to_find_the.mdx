---
title: 'bash command to find the number of files in a directory'
description: 'bash command to find the number of files in a directory'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [bash, command, directory, find, shell, each, dire]
---

**The solution for this is noted below**

## bash command to find the number of files in a directory

### Solution

```shell
ls -1q  | wc -l
```

**Try other methods by searching on the site. That is if this doesn't work**
