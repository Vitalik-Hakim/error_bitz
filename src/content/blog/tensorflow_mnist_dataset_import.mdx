---
title: 'tensorflow mnist dataset import'
description: 'tensorflow mnist dataset import'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [dataset, import, mnist, python, tensorflow]
---

**The solution for this is noted below**

## tensorflow mnist dataset import

### Solution

```python
from tensorflow.keras.datasets import mnist
(X_train, Y_train), (X_test, Y_test) = mnist.load_data()
```

**Try other methods by searching on the site. That is if this doesn't work**
