---
title: 'bash fully unsquash sqfs file'
description: 'bash fully unsquash sqfs file'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [bash, file, shell]
---

**The solution for this is noted below**

## bash fully unsquash sqfs file

### Solution

```shell
# Basic syntax:
unsquashfs -d /path/to/directory file.sqfs

# Note, the output directory "directory" should not exist before this is
#	run, it will be created when you run the command
```

**Try other methods by searching on the site. That is if this doesn't work**
