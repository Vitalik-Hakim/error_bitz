---
title: 'how to uninstall android studio in ubuntu 20.04 using terminal'
description: 'how to uninstall android studio in ubuntu 20.04 using terminal'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [android-studio, shell, terminal, ubuntu-20.04, using]
---

**The solution for this is noted below**

## how to uninstall android studio in ubuntu 20.04 using terminal

### Solution

```shell
rm ~/.local/share/applications/androidstudio.desktop
sudo rm -rf /opt/androsudoid-studio/
rm -rf ~/.android/
rm -rf ~/.AndroidStudio2.1/
rm -rf ~/.AndroidStudio2.3/
rm -rf ~/Android
sudo rm -rf /opt/android-studio-ide-143.2790544-linux.zip
rm -rf ~/.gradle
```

**Try other methods by searching on the site. That is if this doesn't work**
