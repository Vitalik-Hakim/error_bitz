---
title: 'how to check if everything inside a list is unique'
description: 'how to check if everything inside a list is unique'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [list, python, unique]
---

**The solution for this is noted below**

## how to check if everything inside a list is unique

### Solution

```python
len(set(x)) == len(x)

if true :
	print("all values of x are unique")
else:
	print("not all values of x are unique")
```

**Try other methods by searching on the site. That is if this doesn't work**
