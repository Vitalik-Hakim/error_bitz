---
title: 'Encountered null while assembling a row with handleInvalid = `error`. Consider removing nulls from dataset or using handleInvalid = `keep` or `skip`.'
description: 'Encountered null while assembling a row with handleInvalid = `error`. Consider removing nulls from dataset or using handleInvalid = `keep` or `skip`.'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/misc.png'
category: 'whatever'
tags: [dataset, row, using, whatever]
---

**The solution for this is noted below**

## Encountered null while assembling a row with handleInvalid = `error`. Consider removing nulls from dataset or using handleInvalid = `keep` or `skip`.

### Solution

```whatever
assembler.setHandleInvalid("skip").transform(df).show
```

**Try other methods by searching on the site. That is if this doesn't work**
