---
title: 'ubuntu uninstall redis'
description: 'ubuntu uninstall redis'
pubDate: 'Apr 17 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [redis, shell, ubuntu, redi]
---

**The solution for this is noted below**

## ubuntu uninstall redis

### Solution

```shell
sudo apt-get purge --auto-remove redis-server

# Check if there is any packages left
apt list --installed | grep redis
```
**Try other methods by searching on the site. That is if this doesn't work**
