---
title: 'generate random letters and numbers in java'
description: 'generate random letters and numbers in java'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/java.png'
category: 'java'
tags: [generate, java, letters-and-numbers, random]
---

**The solution for this is noted below**

## generate random letters and numbers in java

### Solution

```java
import java.util.Random;

public class Generator {
	public static String generateRandomPassword(int len) {
		String chars = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijk"
          +"lmnopqrstuvwxyz!@#$%&";
		Random rnd = new Random();
		StringBuilder sb = new StringBuilder(len);
		for (int i = 0; i < len; i++)
			sb.append(chars.charAt(rnd.nextInt(chars.length())));
		return sb.toString();
	}
}
```

**Try other methods by searching on the site. That is if this doesn't work**
