---
title: 'how to configure bluetooth on ubuntu command line'
description: 'how to configure bluetooth on ubuntu command line'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [bluetooth, command-line, configure, shell, ubuntu, blue]
---

**The solution for this is noted below**

## how to configure bluetooth on ubuntu command line

### Solution

```shell
'If you are on Ubuntu or an Ubuntu-based distro, run the following: command:
'
    sudo apt-get install bluetooth bluez bluez-tools rfkill.
    sudo rfkill list.
    sudo rfkill unblock bluetooth.
    sudo service bluetooth start.
    sudo apt-get install blueman.
```

**Try other methods by searching on the site. That is if this doesn't work**
