---
title: 'install docker-compose ec2'
description: 'install docker-compose ec2'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [docker-compose, shell, dock]
---

**The solution for this is noted below**

## install docker-compose ec2

### Solution

```shell
sudo curl -L "https://github.com/docker/compose/releases/download/1.28.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
```

**Try other methods by searching on the site. That is if this doesn't work**
