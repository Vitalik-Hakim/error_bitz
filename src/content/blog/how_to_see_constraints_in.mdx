---
title: 'how to see constraints in postgresql'
description: 'how to see constraints in postgresql'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/typescript.png'
category: 'typescript'
tags: [constraints, postgresql, typescript, sql, cons]
---

**The solution for this is noted below**

## how to see constraints in postgresql

### Solution

```typescript
SELECT con.*
       FROM pg_catalog.pg_constraint con
            INNER JOIN pg_catalog.pg_class rel
                       ON rel.oid = con.conrelid
            INNER JOIN pg_catalog.pg_namespace nsp
                       ON nsp.oid = connamespace
       WHERE nsp.nspname = '<schema name>'
             AND rel.relname = '<table name>';
```

**Try other methods by searching on the site. That is if this doesn't work**
