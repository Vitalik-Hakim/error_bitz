---
title: 'python concat list to sql query string'
description: 'python concat list to sql query string'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [concat, list, python, query-string, sql]
---

**The solution for this is noted below**

## python concat list to sql query string

### Solution

```python
l = [1, 5, 8]
sql_query = 'select name from studens where id in (' + ','.join(map(str, l)) + ')'
```

**Try other methods by searching on the site. That is if this doesn't work**
