---
title: 'JSX element implicitly has type `any` because no interface `JSX.IntrinsicElements` exists.ts(7026)'
description: 'JSX element implicitly has type `any` because no interface `JSX.IntrinsicElements` exists.ts(7026)'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [element, interface, jsx, shell]
---

**The solution for this is noted below**

## JSX element implicitly has type `any` because no interface `JSX.IntrinsicElements` exists.ts(7026)

### Solution

```shell
npm i -save -D @types/react

yarn add --dev @types/react
```

**Try other methods by searching on the site. That is if this doesn't work**
