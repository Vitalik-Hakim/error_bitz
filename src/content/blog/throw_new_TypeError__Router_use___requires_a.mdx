---
title: 'throw new TypeError(`Router.use() requires a middleware function but got a ` + gettype(fn))'
description: 'throw new TypeError(`Router.use() requires a middleware function but got a ` + gettype(fn))'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/go.png'
category: 'go'
tags: [function, go, got, middleware, requires, throw]
---

**The solution for this is noted below**

## throw new TypeError(`Router.use() requires a middleware function but got a ` + gettype(fn))

### Solution

```go
module.exports = router;
```

**Try other methods by searching on the site. That is if this doesn't work**
