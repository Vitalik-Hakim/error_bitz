---
title: 'convert iformfile to byte array c#'
description: 'convert iformfile to byte array c#'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/c%23.png'
category: 'csharp'
tags: [byte, c#, iformfile, csharp]
---

**The solution for this is noted below**

## convert iformfile to byte array c#

### Solution

```csharp
foreach (var file in files)
{
  if (file.Length > 0)
  {
    using (var ms = new MemoryStream())
    {
      file.CopyTo(ms);
      var fileBytes = ms.ToArray();
    }
  }
}
```

**Try other methods by searching on the site. That is if this doesn't work**
