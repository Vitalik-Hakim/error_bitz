---
title: 'pd if value delete row'
description: 'pd if value delete row'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [delete-row, python, pandas, pa, dataframe, data, whatever]
---

**The solution for this is noted below**

## pd if value delete row

### Solution

```python
df.drop(df.loc[df['line_race']==0].index, inplace=True)
```

**Try other methods by searching on the site. That is if this doesn't work**
