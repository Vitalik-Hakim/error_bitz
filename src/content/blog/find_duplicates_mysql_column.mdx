---
title: 'find duplicates mysql column'
description: 'find duplicates mysql column'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/sql.png'
category: 'sql'
tags: [find, mysql, sql, mysq, multiple-columns, multip, sq]
---

**The solution for this is noted below**

## find duplicates mysql column

### Solution

```sql
SELECT name, COUNT(*) c FROM table GROUP BY name HAVING c > 1;
```

**Try other methods by searching on the site. That is if this doesn't work**
