---
title: 'update all packages in package.json'
description: 'update all packages in package.json'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/JavaScript.png'
category: 'javascript'
tags: [javascript, package.json, update-all, shell, package.js, whatever, j]
---

**The solution for this is noted below**

## update all packages in package.json

### Solution

```javascript
//To install major updates globally
npm install -g npm-check-updates

//To see a list of outdated packages
npm outdated

//To update the packages to their latest versions in package.json
ncu -u

//To install the new updates
npm update 	or 	npm install
```

**Try other methods by searching on the site. That is if this doesn't work**
