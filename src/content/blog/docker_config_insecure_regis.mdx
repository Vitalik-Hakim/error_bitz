---
title: 'docker config insecure regis'
description: 'docker config insecure regis'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [config, docker, shell]
---

**The solution for this is noted below**

## docker config insecure regis

### Solution

```shell
To add an insecure docker registry, add the file /etc/docker/daemon.json with the following content:

{
    "insecure-registries" : [ "hostname.cloudapp.net:5000" ]
}
```

**Try other methods by searching on the site. That is if this doesn't work**
