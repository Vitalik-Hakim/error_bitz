---
title: 'Mysql query add column with default string value'
description: 'Mysql query add column with default string value'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/sql.png'
category: 'sql'
tags: [add, default, mysql, sql, string]
---

**The solution for this is noted below**

## Mysql query add column with default string value

### Solution

```sql
ALTER TABLE `table_name` ADD COLUMN `new_column_name` VARCHAR(255)  DEFAULT "DEFUALT_VALUE" AFTER `COLUMN_NAME`;
```

**Try other methods by searching on the site. That is if this doesn't work**
