---
title: 'object to query string js'
description: 'object to query string js'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/JavaScript.png'
category: 'javascript'
tags: [javascript, object, query-string, j, typescript, ob]
---

**The solution for this is noted below**

## object to query string js

### Solution

```javascript
const obj = { foo: 'hi there', bar: '100%' }
const params = new URLSearchParams(obj).toString()
```

**Try other methods by searching on the site. That is if this doesn't work**
