---
title: 'ubuntu install php-xml'
description: 'ubuntu install php-xml'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/php.png'
category: 'php'
tags: [php, ubuntu, command, shell, apt, sudo]
---

**The solution for this is noted below**

## ubuntu install php-xml

### Solution

```php
# PHP 7.1
sudo apt install php7.1-xml

# PHP 7.2:
sudo apt install php7.2-xml

# PHP 7.3
sudo apt install php7.3-xml


# PHP 7.4
sudo apt install php7.4-xml

# PHP 8
sudo apt install php-xml
```

**Try other methods by searching on the site. That is if this doesn't work**
