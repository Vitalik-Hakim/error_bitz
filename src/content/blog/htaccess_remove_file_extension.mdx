---
title: 'htaccess remove file extension'
description: 'htaccess remove file extension'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/misc.png'
category: 'whatever'
tags: [file-extension, whatever]
---

**The solution for this is noted below**

## htaccess remove file extension

### Solution

```whatever
RewriteEngine on
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.html -f
RewriteRule ^(.*)$ $1.html [NC,L]
```

**Try other methods by searching on the site. That is if this doesn't work**
