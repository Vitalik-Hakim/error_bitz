---
title: 'postgresql calculate age from birthdate'
description: 'postgresql calculate age from birthdate'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/sql.png'
category: 'sql'
tags: [postgresql, sql, p]
---

**The solution for this is noted below**

## postgresql calculate age from birthdate

### Solution

```sql
SELECT EXTRACT(YEAR from AGE(NOW(), birth)) as "age", * FROM customers;
```

**Try other methods by searching on the site. That is if this doesn't work**
