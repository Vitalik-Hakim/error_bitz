---
title: 'add column if not exists postgresql'
description: 'add column if not exists postgresql'
pubDate: 'Apr 17 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/typescript.png'
category: 'typescript'
tags: [add, not-exists, postgresql, typescript]
---

**The solution for this is noted below**

## add column if not exists postgresql

### Solution

```typescript
ALTER TABLE table_name ADD COLUMN IF NOT EXISTS column_name INTEGER;
```
**Try other methods by searching on the site. That is if this doesn't work**
