---
title: 'linux change hostname'
description: 'linux change hostname'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [hostname, linux, shell, whatever, hostnam, centos, ce, bash, host]
---

**The solution for this is noted below**

## linux change hostname

### Solution

```shell
sudo nano /etc/hostname
sudo nano /etc/hosts
sudo reboot
```

**Try other methods by searching on the site. That is if this doesn't work**
