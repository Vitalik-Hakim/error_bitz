---
title: 'spring shell inside docker'
description: 'spring shell inside docker'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [docker, shell, spring-shell]
---

**The solution for this is noted below**

## spring shell inside docker

### Solution

```shell
Docker run command has to include -it
-t              : Allocate a pseudo-tty
-i              : Keep STDIN open even if not attached
```

**Try other methods by searching on the site. That is if this doesn't work**
