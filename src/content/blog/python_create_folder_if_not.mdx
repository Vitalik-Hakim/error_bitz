---
title: 'python create folder if not exists'
description: 'python create folder if not exists'
pubDate: 'Apr 17 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [not-exists, python, create-directory, crea, directory, dire, path, javascript, py]
---

**The solution for this is noted below**

## python create folder if not exists

### Solution

```python
if (not os.path.exists("images")):
    os.mkdir("images")
```
**Try other methods by searching on the site. That is if this doesn't work**
