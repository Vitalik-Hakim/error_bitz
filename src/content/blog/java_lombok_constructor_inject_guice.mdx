---
title: 'java lombok constructor inject guice'
description: 'java lombok constructor inject guice'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/java.png'
category: 'java'
tags: [constructor, guice, inject, java, lombok]
---

**The solution for this is noted below**

## java lombok constructor inject guice

### Solution

```java
@AllArgsConstructor(onConstructor = @__(@Inject))
@RequiredArgsConstructor(onConstructor = @__(@Inject))
```

**Try other methods by searching on the site. That is if this doesn't work**
