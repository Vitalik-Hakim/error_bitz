---
title: 'react iterate number of times'
description: 'react iterate number of times'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/JavaScript.png'
category: 'javascript'
tags: [iterate, javascript]
---

**The solution for this is noted below**

## react iterate number of times

### Solution

```javascript
const n = 8 // Or something else

Array.from(Array(n)).map((e, i) => (
	<span className='busterCards' key={i}>
		♦
	</span>
))
```

**Try other methods by searching on the site. That is if this doesn't work**
