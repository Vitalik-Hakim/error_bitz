---
title: 'npm port already in use'
description: 'npm port already in use'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [npm, port, shell, whatever, npm-start, npm-]
---

**The solution for this is noted below**

## npm port already in use

### Solution

```shell
$ lsof -i tcp:3000
$ kill -9 PID
```

**Try other methods by searching on the site. That is if this doesn't work**
