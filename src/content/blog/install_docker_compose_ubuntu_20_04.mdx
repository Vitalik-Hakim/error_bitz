---
title: 'install docker-compose ubuntu 20.04'
description: 'install docker-compose ubuntu 20.04'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [docker-compose, shell, ubuntu-20.04, dock]
---

**The solution for this is noted below**

## install docker-compose ubuntu 20.04

### Solution

```shell
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose && sudo chmod +x /usr/local/bin/docker-compose
```

**Try other methods by searching on the site. That is if this doesn't work**
