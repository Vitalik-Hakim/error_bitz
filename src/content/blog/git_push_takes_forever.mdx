---
title: 'git push takes forever'
description: 'git push takes forever'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [forever, git-push, shell, shel]
---

**The solution for this is noted below**

## git push takes forever

### Solution

```shell
for me the issue was in branch name,
I was pushing to main branch on remote from master on local,
so I've created a main branch it all worked!
```

**Try other methods by searching on the site. That is if this doesn't work**
