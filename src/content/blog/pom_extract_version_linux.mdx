---
title: 'pom extract version linux'
description: 'pom extract version linux'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [extract, linux, shell, version]
---

**The solution for this is noted below**

## pom extract version linux

### Solution

```shell
mvn -q -Dexec.executable=echo -Dexec.args='${project.version}' --non-recursive exec:exec
```

**Try other methods by searching on the site. That is if this doesn't work**
