---
title: 'django api sort fields'
description: 'django api sort fields'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [api, django, python]
---

**The solution for this is noted below**

## django api sort fields

### Solution

```python
# Sorting is done on the queryset in your views
class exampleViewSet(viewsets.ModelViewSet):
    serializer_class = exampleSerializer
    queryset = example.objects.all().order_by("columnA", "-columnB")
```

**Try other methods by searching on the site. That is if this doesn't work**
