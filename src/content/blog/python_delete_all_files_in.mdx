---
title: 'python delete all files in directory'
description: 'python delete all files in directory'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [directory, python, dire]
---

**The solution for this is noted below**

## python delete all files in directory

### Solution

```python
import os

filelist = [ f for f in os.listdir(mydir) if f.endswith(".bak") ]
for f in filelist:
    os.remove(os.path.join(mydir, f))
```

**Try other methods by searching on the site. That is if this doesn't work**
