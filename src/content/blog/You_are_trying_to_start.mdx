---
title: 'You are trying to start Visual Studio Code as a super user which isn`t recommended. If this was intended, please add the argument `--no-sandbox` and specify an alternate user data directory using the `--user-data-dir` argument.'
description: 'You are trying to start Visual Studio Code as a super user which isn`t recommended. If this was intended, please add the argument `--no-sandbox` and specify an alternate user data directory using the `--user-data-dir` argument.'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags:
  [
    add,
    alternate,
    directory,
    super,
    this,
    user-data,
    using,
    visual-studio,
    was,
    whatever,
    assembly,
    alternat
  ]
---

**The solution for this is noted below**

## You are trying to start Visual Studio Code as a super user which isn`t recommended. If this was intended, please add the argument `--no-sandbox`and specify an alternate user data directory using the`--user-data-dir` argument.

### Solution

```shell
sudo code "your-directory" --user-data-dir='.' --no-sandbox
```

**Try other methods by searching on the site. That is if this doesn't work**
