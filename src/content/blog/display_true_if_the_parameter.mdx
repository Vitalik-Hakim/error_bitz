---
title: 'display true if the parameter is type string'
description: 'display true if the parameter is type string'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/JavaScript.png'
category: 'javascript'
tags: [display, javascript, string]
---

**The solution for this is noted below**

## display true if the parameter is type string

### Solution

```javascript
if (typeof myVar === 'string' || myVar instanceof String)
// it's a string
else
// it's something else
```

**Try other methods by searching on the site. That is if this doesn't work**
