---
title: 'Sort an array using setTimeout function'
description: 'Sort an array using setTimeout function'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/JavaScript.png'
category: 'javascript'
tags: [function, javascript, settimeout, using]
---

**The solution for this is noted below**

## Sort an array using setTimeout function

### Solution

```javascript
let arr = [10, 100, 500, 20, 35]

arr.forEach((item) => {
	setTimeout(() => console.log(item), item)
})
```

**Try other methods by searching on the site. That is if this doesn't work**
