---
title: 'js replace item in array at index'
description: 'js replace item in array at index'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/typescript.png'
category: 'typescript'
tags: [replace, typescript]
---

**The solution for this is noted below**

## js replace item in array at index

### Solution

```typescript
array.splice(indexWhereToReplace, 1, replacement)

// Ex
const arr = [1, 2, 3]
arr.splice(1, 1, 'replacement')
console.log(arr) // [1, 'replacement', 3]
```

**Try other methods by searching on the site. That is if this doesn't work**
