---
title: 'name `redirect` is not defined django'
description: 'name `redirect` is not defined django'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [defined, django, python]
---

**The solution for this is noted below**

## name `redirect` is not defined django

### Solution

```python
For django python add this to top of your code

from django.shortcuts import redirect
```

**Try other methods by searching on the site. That is if this doesn't work**
