---
title: 'for column in cleaned_df.columns:     if cleaned_df[column].dtype == np.number:         continue         cleaned_df[column] = LabelEncoder().fit_trasform(cleaned_df[column])'
description: 'for column in cleaned_df.columns:     if cleaned_df[column].dtype == np.number:         continue         cleaned_df[column] = LabelEncoder().fit_trasform(cleaned_df[column])'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [continue, python]
---

**The solution for this is noted below**

## for column in cleaned_df.columns: if cleaned_df[column].dtype == np.number: continue cleaned_df[column] = LabelEncoder().fit_trasform(cleaned_df[column])

### Solution

```python
for column in cleaned_df.columns:
    if cleaned_df[column].dtype == np.number:
        continue
        cleaned_df[column] = LabelEncoder().fit_trasform(cleaned_df[column])
```

**Try other methods by searching on the site. That is if this doesn't work**
