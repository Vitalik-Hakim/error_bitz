---
title: 'c# get all bytes of a file'
description: 'c# get all bytes of a file'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/c%23.png'
category: 'csharp'
tags: [c#, file, get]
---

**The solution for this is noted below**

## c# get all bytes of a file

### Solution

```csharp
//replace filepath with the real filepath
byte[] fileBytes = System.IO.File.ReadAllBytes(filepath);
```

**Try other methods by searching on the site. That is if this doesn't work**
