---
title: 'get file extension from base64 string'
description: 'get file extension from base64 string'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/C.png'
category: 'c'
tags: [base64, c, file-extension, get, string]
---

**The solution for this is noted below**

## get file extension from base64 string

### Solution

```c
extension = base64_data.split(';')[0].split('/')[1]
```

**Try other methods by searching on the site. That is if this doesn't work**
