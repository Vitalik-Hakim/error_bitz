---
title: 'jquery validate if field exists'
description: 'jquery validate if field exists'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/JavaScript.png'
category: 'javascript'
tags: [exists, field, javascript, jquery-validate, jquery]
---

**The solution for this is noted below**

## jquery validate if field exists

### Solution

```javascript
if ($('#selector').length) {
	// use this if you are using id to check
	// it exists
}

if ($('.selector').length) {
	// use this if you are using class to check
	// it exists
}
```

**Try other methods by searching on the site. That is if this doesn't work**
