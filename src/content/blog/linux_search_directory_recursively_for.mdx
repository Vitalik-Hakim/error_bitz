---
title: 'linux search directory recursively for all files with extension'
description: 'linux search directory recursively for all files with extension'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [directory, linux, search, shell, ubuntu, get]
---

**The solution for this is noted below**

## linux search directory recursively for all files with extension

### Solution

```shell
find "$directory" -type f -name "*.in"

# Example: To find all csv files in the current directory and its sub-directories, use:
find . -type f -name '*.csv'
```

**Try other methods by searching on the site. That is if this doesn't work**
