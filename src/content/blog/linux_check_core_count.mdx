---
title: 'linux check core count'
description: 'linux check core count'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [core, count, linux, shell]
---

**The solution for this is noted below**

## linux check core count

### Solution

```shell
# this should show count of processor cores
cat /proc/cpuinfo | grep -i processor | wc -l
```

**Try other methods by searching on the site. That is if this doesn't work**
