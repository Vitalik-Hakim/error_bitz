---
title: 'django model specify table name'
description: 'django model specify table name'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [django, model, python]
---

**The solution for this is noted below**

## django model specify table name

### Solution

```python
class TempUser(models.Model):
    first_name = models.CharField(max_length=100)
    // ....
    class Meta:
        db_table = "tbl_temp_users"
```

**Try other methods by searching on the site. That is if this doesn't work**
