---
title: 'return maximum of three values in python'
description: 'return maximum of three values in python'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [python, return]
---

**The solution for this is noted below**

## return maximum of three values in python

### Solution

```python
def maximum(a,b,c):
  max_val = a
  if b > max_val:
    max_val = b
  if c > max_val:
    max_val = c
  return max_val
```

**Try other methods by searching on the site. That is if this doesn't work**
