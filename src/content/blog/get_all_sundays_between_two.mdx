---
title: 'get all sundays between two dates c#'
description: 'get all sundays between two dates c#'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/c%23.png'
category: 'csharp'
tags: [between, c#, get]
---

**The solution for this is noted below**

## get all sundays between two dates c#

### Solution

```csharp
IEnumerable<DateTime> GetDaysBetween(DateTime start, DateTime end)
{
    for (DateTime i = start; i < end; i = i.AddDays(1))
    {
        yield return i;
    }
}

var weekends = GetDaysBetween(DateTime.Today, DateTime.Today.AddDays(365))
    .Where(d => d.DayOfWeek == DayOfWeek.Saturday && d.DayOfWeek == DayOfWeek.Sunday);
```

**Try other methods by searching on the site. That is if this doesn't work**
