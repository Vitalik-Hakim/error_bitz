---
title: 'Failed to start ssh.service: Unit ssh.service not found.'
description: 'Failed to start ssh.service: Unit ssh.service not found.'
pubDate: 'Apr 17 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/CSS.png'
category: 'css'
tags: [css, shell]
---

**The solution for this is noted below**

## Failed to start ssh.service: Unit ssh.service not found.

### Solution

```css
sudo apt-get install openssh-server
# Source
# https://unix.stackexchange.com/questions/520341/ssh-installed-but-i-get-the-error-failed-to-start-ssh-service-unit-ssh-service

# start SSH
sudo systemctl start ssh

# connect host server
sudo root@server-address -p password
  
exit
```
**Try other methods by searching on the site. That is if this doesn't work**
