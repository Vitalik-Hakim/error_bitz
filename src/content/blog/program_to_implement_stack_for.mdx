---
title: 'program to implement stack for book details(book no, book name). implement push and display operation'
description: 'program to implement stack for book details(book no, book name). implement push and display operation'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/misc.png'
category: 'whatever'
tags: [display, operation, push, stack, whatever]
---

**The solution for this is noted below**

## program to implement stack for book details(book no, book name). implement push and display operation

### Solution

```whatever
def push(Books):
	Stack.append(Books)
	print 'Element:', Book, 'inserted successfully'
def pop():
	if Stack == []:
		print('Stack is empty!')
	else:
		print('Deleted element is',Stack.pop())
```

**Try other methods by searching on the site. That is if this doesn't work**
