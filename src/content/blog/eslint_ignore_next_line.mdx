---
title: 'eslint ignore next line'
description: 'eslint ignore next line'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/JavaScript.png'
category: 'javascript'
tags: [eslint, ignore, javascript, line, next, es, disable, j]
---

**The solution for this is noted below**

## eslint ignore next line

### Solution

```javascript
alert('foo') // eslint-disable-line

// eslint-disable-next-line
alert('foo')

/* eslint-disable-next-line */
alert('foo')

alert('foo') /* eslint-disable-line */
```

**Try other methods by searching on the site. That is if this doesn't work**
