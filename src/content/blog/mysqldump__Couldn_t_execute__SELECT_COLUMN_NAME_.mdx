---
title: 'mysqldump: Couldn`t execute `SELECT COLUMN_NAME,'
description: 'mysqldump: Couldn`t execute `SELECT COLUMN_NAME,'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/sql.png'
category: 'sql'
tags: [execute, sql]
---

**The solution for this is noted below**

## mysqldump: Couldn`t execute `SELECT COLUMN_NAME,

### Solution

```sql
mysqldump --column-statistics=0 --host=<server> --user=<user> --password=<password>
```

**Try other methods by searching on the site. That is if this doesn't work**
