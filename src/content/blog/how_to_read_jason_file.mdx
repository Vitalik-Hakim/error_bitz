---
title: 'how to read jason file in jupyter notebook'
description: 'how to read jason file in jupyter notebook'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/misc.png'
category: 'whatever'
tags: [file, jupyter-notebook, whatever, fil]
---

**The solution for this is noted below**

## how to read jason file in jupyter notebook

### Solution

```whatever
import numpy as np
import pandas as pd
import seaborn as sns
import matplotlib as plt
import json
%matplotlib inline

with open("pud.json") as datafile:
  data = json.load(datafile)
dataframe = pd.DataFrame(data)
```

**Try other methods by searching on the site. That is if this doesn't work**
