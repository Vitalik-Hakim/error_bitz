---
title: 'could not resolve a module for a custom reporter. module name jest-junit'
description: 'could not resolve a module for a custom reporter. module name jest-junit'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/misc.png'
category: 'whatever'
tags: [module, resolve, whatever]
---

**The solution for this is noted below**

## could not resolve a module for a custom reporter. module name jest-junit

### Solution

```whatever
npm install jest --save-dev
npm install jest-junit --save-dev
```

**Try other methods by searching on the site. That is if this doesn't work**
