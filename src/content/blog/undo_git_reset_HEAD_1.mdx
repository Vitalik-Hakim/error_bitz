---
title: 'undo git reset HEAD~1'
description: 'undo git reset HEAD~1'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/SHELL.jpg'
category: 'shell'
tags: [git-reset, shell, undo, git-, reset, shel]
---

**The solution for this is noted below**

## undo git reset HEAD~1

### Solution

```shell
# if you would like to move back the head by one position you could do:
git reset HEAD~

# if you want to move multiple positions at once e.g 3 times back type:
git reset HEAD@{3}
```

**Try other methods by searching on the site. That is if this doesn't work**
