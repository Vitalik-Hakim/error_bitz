---
title: 'module `tensorflow` has no attribute `reset_default_graph`'
description: 'module `tensorflow` has no attribute `reset_default_graph`'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [module, python, mo, py]
---

**The solution for this is noted below**

## module `tensorflow` has no attribute `reset_default_graph`

### Solution

```python
import tensorflow as tf
tf.compat.v1.reset_default_graph()
```

**Try other methods by searching on the site. That is if this doesn't work**
