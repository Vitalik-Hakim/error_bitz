---
title: 'regex in python to obtain only the string in python'
description: 'regex in python to obtain only the string in python'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [python, regex, string, get]
---

**The solution for this is noted below**

## regex in python to obtain only the string in python

### Solution

```python
import re
print(" ".join(re.findall("[a-zA-Z]","your string")))
```

**Try other methods by searching on the site. That is if this doesn't work**
