---
title: 'npm install moment in vue'
description: 'npm install moment in vue'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/JavaScript.png'
category: 'javascript'
tags: [npm-install, shell, shel]
---

**The solution for this is noted below**

## npm install moment in vue

### Solution

```javascript
//We can use for date format
npm install moment
//Or
npm install moment --save
//Or
npm i moment --save

//Use in vue
import moment from 'moment';

{{ moment(user.created_id).format('YYYY-MM-DD') }}
```

**Try other methods by searching on the site. That is if this doesn't work**
