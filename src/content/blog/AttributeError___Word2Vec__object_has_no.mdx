---
title: 'AttributeError: `Word2Vec` object has no attribute `most_similar`'
description: 'AttributeError: `Word2Vec` object has no attribute `most_similar`'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [object, python]
---

**The solution for this is noted below**

## AttributeError: `Word2Vec` object has no attribute `most_similar`

### Solution

```python
model.wv.most_similar("glass")

add wv in between
```

**Try other methods by searching on the site. That is if this doesn't work**
