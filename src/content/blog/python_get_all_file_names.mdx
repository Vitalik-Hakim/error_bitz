---
title: 'python get all file names in a dir'
description: 'python get all file names in a dir'
pubDate: 'Apr 16 2024'
heroImage: 'https://raw.githubusercontent.com/vitalik-hakim/TABLES404/main/error_b/python.jpg'
category: 'python'
tags: [dir, file, get, names, python, directory, dire]
---

**The solution for this is noted below**

## python get all file names in a dir

### Solution

```python
from os import listdir
from os.path import isfile, join
onlyfiles = [f for f in listdir(mypath) if isfile(join(mypath, f))]
```

**Try other methods by searching on the site. That is if this doesn't work**
